# Port for the application (Render will set this automatically)
PORT=3000

# URLs to ping (comma-separated list)
# Add your Render app URLs here
PING_URLS=https://your-app-1.onrender.com,https://your-app-2.onrender.com,https://your-app-3.onrender.com

# Webhook URLs for notifications (comma-separated list) - OPTIONAL
# Discord webhook example:
# WEBHOOK_URLS=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Slack webhook example:
# WEBHOOK_URLS=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Multiple webhooks example:
# WEBHOOK_URLS=https://discord.com/api/webhooks/123/abc,https://hooks.slack.com/services/xyz/def

# Generic webhook example:
# WEBHOOK_URLS=https://your-custom-endpoint.com/webhooks/ping-alerts

# Notify on success (optional, default: false - only notifies on failures)
# NOTIFY_SUCCESS=true

# Example with 3 apps (as requested):
# PING_URLS=https://app1.onrender.com,https://app2.onrender.com,https://app3.onrender.com   